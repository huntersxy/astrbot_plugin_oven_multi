{
  "blacklist_groups": {
    "description": "黑名单群组",
    "hint": "不启用插件的群号码列表",
    "type": "list",
    "default": []
  },
  "blacklist_users": {
    "description": "黑名单用户",
    "hint": "不启用插件的 QQ 用户号码列表",
    "type": "list",
    "default": []
  },
  "bracket_matching": {
    "description": "括号自动匹配功能",
    "hint": "自动补全缺失的括号，让消息中的括号成对出现",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用括号自动匹配功能",
        "hint": "开启后自动检测并补全消息中的括号",
        "type": "bool",
        "default": true
      },
      "check_group_messages": {
        "description": "检查群组消息",
        "hint": "是否检查群组消息中的括号匹配",
        "type": "bool",
        "default": true
      },
      "check_private_messages": {
        "description": "检查私聊消息",
        "hint": "是否检查私聊消息中的括号匹配",
        "type": "bool",
        "default": false
      }
    }
  },
  "repetition": {
    "description": "消息复读功能",
    "hint": "有概率复读相同的消息，增加趣味性",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用消息复读功能",
        "hint": "开启后有概率复读相同的消息",
        "type": "bool",
        "default": true
      },
      "break_spell_probability": {
        "description": "打断施法概率",
        "hint": "复读时触发打断施法的概率 (0-1)",
        "type": "float",
        "slider": {
          "min": 0,
          "max": 1,
          "step": 0.01
        },
        "default": 0.3
      },
      "break_spell_text": {
        "description": "打断施法文本",
        "hint": "打断施法时显示的文本",
        "type": "string",
        "default": "打断施法！"
      },
      "repeat_threshold": {
        "description": "复读触发阈值",
        "hint": "相同消息出现多少次后触发复读",
        "type": "int",
        "slider": {
          "min": 1,
          "max": 10,
          "step": 1
        },
        "default": 2
      }
    }
  },
  "remove_blank_lines": {
    "description": "移除空行功能",
    "hint": "自动清理机器人回复中的多余空行",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用移除空行功能",
        "hint": "开启后自动清理机器人回复中的多余空行",
        "type": "bool",
        "default": true
      },
      "max_consecutive_newlines": {
        "description": "最大连续换行数",
        "hint": "保留的最大连续换行符数量",
        "type": "int",
        "slider": {
          "min": 0,
          "max": 10,
          "step": 1
        },
        "default": 1
      }
    }
  },
  "iam_thinking": {
    "description": "思考表情功能",
    "hint": "在处理LLM请求时自动贴表情提示用户正在思考",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用思考表情功能",
        "hint": "开启后在处理LLM请求时自动贴表情",
        "type": "bool",
        "default": true
      },
      "thinking_emoji_ids": {
        "description": "思考中表情ID列表",
        "hint": "等待LLM响应时贴的表情ID列表",
        "type": "list",
        "default": [66]
      },
      "done_emoji_ids": {
        "description": "完成表情ID列表",
        "hint": "LLM响应完成后贴的表情ID列表",
        "type": "list",
        "default": [74]
      },
      "remove_thinking_on_done": {
        "description": "完成后移除思考中表情",
        "hint": "LLM响应完成后是否移除思考中表情",
        "type": "bool",
        "default": true
      },
      "add_done_emoji": {
        "description": "完成后添加完成表情",
        "hint": "LLM响应完成后是否添加完成表情",
        "type": "bool",
        "default": true
      }
    }
  },
  "style_learning": {
    "description": "风格学习功能",
    "hint": "从聊天中学习群聊的说话风格和内部梗，并注入到LLM回复中",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用风格学习功能",
        "hint": "开启后自动收集聊天记录并定期分析学习风格特征",
        "type": "bool",
        "default": true
      },
      "enable_style_injection": {
        "description": "启用风格注入",
        "hint": "是否自动将学习到的风格注入到机器人的回复中",
        "type": "bool",
        "default": true
      },
      "style_provider_id": {
        "type": "string",
        "description": "风格注入目标 Provider ID",
        "_special": "select_provider",
        "default": "",
        "hint": "选择风格分析使用的 LLM Provider。留空则使用当前会话默认 Provider。和参考插件用法一致。"
      },
      "analysis_interval_seconds": {
        "description": "聊天记录分析频率（秒）",
        "hint": "每隔多少秒对积累的聊天记录进行一次学习分析。默认值为3600（1小时）。",
        "type": "int",
        "default": 3600
      },
      "maintenance_interval_seconds": {
        "description": "风格维护任务频率（秒）",
        "hint": "每隔多少秒进行一次表征合并和容量清理。默认值为86400（24小时）。",
        "type": "int",
        "default": 86400
      },
      "min_history_for_analysis": {
        "description": "触发分析的最少消息数",
        "hint": "一个会话的聊天记录达到此数量时才会被纳入学习分析。默认值为10。",
        "type": "int",
        "default": 10
      },
      "max_specific_per_session": {
        "description": "每个会话的最大特定表征容量",
        "hint": "每个会话最多可以存储多少条特定表征。默认值为200。",
        "type": "int",
        "default": 200
      },
      "specific_promotion_threshold": {
        "description": "特定表征升格为通用的触发次数阈值",
        "hint": "特定表征触发次数达到此值时将在下次分析时提示LLM考虑纳入通用表征。默认值为5。",
        "type": "int",
        "default": 5
      },
      "max_contextual_per_session": {
        "description": "每个会话的最大情境表征容量",
        "hint": "每个会话最多可以存储多少条情境表征。默认值为50。",
        "type": "int",
        "default": 50
      }
    }
  },
  "mem0": {
    "description": "mem0 长期记忆功能",
    "hint": "使用 mem0 托管服务实现长期记忆，自动保存对话并检索相关记忆注入到 LLM 回复中",
    "type": "object",
    "items": {
      "enable": {
        "description": "启用 mem0 长期记忆",
        "hint": "开启后自动保存对话并检索相关记忆",
        "type": "bool",
        "default": true
      },
      "mem0_api_key": {
        "description": "mem0 API Key",
        "hint": "mem0 托管服务的 API Key。请勿公开或发给不可信的人。",
        "type": "string",
        "default": ""
      },
      "mem0_api_base": {
        "description": "mem0 API 地址",
        "hint": "mem0 服务的完整 API 地址",
        "type": "string",
        "default": ""
      },
      "memory_scope": {
        "description": "记忆作用域",
        "hint": "session 表示按平台会话分别存储记忆；sender 表示按发送者 ID 存储记忆。",
        "type": "string",
        "options": ["session", "sender"],
        "default": "session"
      },
      "user_id_prefix": {
        "description": "mem0 用户 ID 前缀",
        "hint": "用于区分不同实例的记忆命名空间",
        "type": "string",
        "default": "astrbot"
      },
      "agent_id": {
        "description": "Agent ID",
        "hint": "用于标识不同的AI代理或实例，可选。",
        "type": "string",
        "default": ""
      },
      "search_limit": {
        "description": "最多注入记忆数量",
        "hint": "每次 LLM 请求最多检索多少条相关记忆",
        "type": "int",
        "default": 5
      },
      "search_threshold": {
        "description": "检索相关性阈值",
        "hint": "记忆检索的相关性阈值（越低越宽松）",
        "type": "float",
        "default": 0.1
      },
      "max_memory_chars": {
        "description": "单条记忆最大字符数",
        "hint": "超过此长度的记忆会被截断",
        "type": "int",
        "default": 240
      },
      "ignore_commands": {
        "description": "忽略命令消息",
        "hint": "开启时跳过以 / 开头的命令消息，不检索/保存记忆",
        "type": "bool",
        "default": true
      },
      "request_timeout": {
        "description": "请求超时时间（秒）",
        "hint": "mem0 API 请求超时时间",
        "type": "float",
        "default": 12
      }
    }
  },
  "active_reply": {
    "description": "主动回复",
    "hint": "在群聊中主动回复（无需被 @）。无对话时自动创建。",
    "type": "object",
    "items": {
      "enable": {
        "description": "启用",
        "hint": "启用主动回复。建议关闭 AstrBot 内置主动回复与内置引用回复。",
        "type": "bool",
        "default": false
      },
      "mode": {
        "description": "触发模式",
        "hint": "可选 probability（概率触发）或 model_choice（模型选择触发）。",
        "type": "string",
        "default": "probability"
      },
      "possibility": {
        "description": "回复概率",
        "hint": "当 mode=probability 时生效，范围建议 0.0 - 1.0。",
        "type": "float",
        "default": 0.1
      },
      "model_stack_size": {
        "description": "模型触发栈长度",
        "hint": "当 mode=model_choice 时生效，累计到该条数后触发一次模型判定。",
        "type": "int",
        "default": 8
      },
      "model_history_messages": {
        "description": "模型判定历史条数",
        "hint": "当 mode=model_choice 时生效，判定时附带的额外历史消息条数（0 表示不附带）。",
        "type": "int",
        "default": 0
      },
      "model_choice_provider_id": {
        "description": "模型判定提供商 ID",
        "hint": "当 mode=model_choice 时生效。用于判定是否主动回复的 LLM Provider。留空使用当前会话默认。",
        "type": "string",
        "_special": "select_provider",
        "default": ""
      },
      "model_choice_prompt": {
        "description": "模型判定提示词",
        "hint": "可使用 {stack_size}、{messages}、{history_count}、{history_context} 占位符，模型需只输出 REPLY 或 SKIP。",
        "type": "string",
        "default": "你正在群聊中扮演助手。以下是最近 {stack_size} 条群聊消息：\n{messages}\n\n额外历史上下文（最近 {history_count} 条）：\n{history_context}\n\n请判断你是否应该主动回复。如果需要回复，只输出 REPLY；如果不需要，只输出 SKIP。"
      },
      "whitelist": {
        "description": "白名单",
        "hint": "英文逗号分隔的 unified_msg_origin 或群 ID。留空表示所有群可触发。",
        "type": "string",
        "default": ""
      }
    }
  },
  "balance": {
    "description": "余额查询",
    "hint": "通过 API 查询各服务商余额，仅在 Dashboard 页面查看。",
    "type": "object",
    "items": {
      "enable": {
        "description": "启用",
        "hint": "启用余额查询功能。",
        "type": "bool",
        "default": false
      },
      "config_content": {
        "description": "API配置内容",
        "type": "text",
        "editor_mode": true,
        "editor_language": "yaml",
        "editor_theme": "vs-dark",
        "hint": "用于配置如何解析余额。内置类型只需 type + api_key；自定义需 url + headers + result_template（{{...}} 格式）",
        "default": "# 内置类型示例（只需 type 和 api_key）\nservices:\n  deepseek:\n    type: deepseek\n    api_key: \"your_api_key\"\n  siliconflow:\n    type: siliconflow\n    api_key: \"your_api_key\"\n\n# 自定义示例（需要完整配置）\n#  custom_service:\n#    url: \"https://api.example.com/balance\"\n#    headers:\n#      Authorization: \"Bearer your_api_key\"\n#    result_template: \"余额: {{data.balance}} 元\""
      },
      "config_mode": {
        "description": "配置模式",
        "type": "string",
        "options": ["simple", "yaml"],
        "default": "yaml"
      }
    }
  }
}