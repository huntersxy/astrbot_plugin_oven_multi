# 2026-06-29

## 代码审查标准与流程建立

为 `astrbot_plugin_oven_multi` 项目建立了完整的代码审查机制：

- 创建了 `CODE_REVIEW_GUIDE.md`：包含审查优先级定义、6 大审查维度（安全性/正确性/可维护性/性能/测试/风格一致性）、27 条检查项、v1.38.0 首次审查结果、改进路线图
- 首次审查发现 3 个 Blocker：`eval()` 安全漏洞（balance_checker.py:289）、`_is_enabled` 重复定义（main.py:191+620）、`asyncio.create_task()` 返回值未保存
- 创建了 GitHub 模板（PR 模板、Bug 报告、功能建议）
- 配置了 CI 流水线（Ruff lint + pytest）
- 编写了 pyproject.toml 统一工具配置
- 创建了 6 个测试文件、约 100 个测试用例覆盖核心模块（bracket_matcher、style_selector、config_manager、mention_parser、balance_template、learning_manager_json）
