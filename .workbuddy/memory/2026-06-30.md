# 2026-06-30

## 修复 balance_checker.py eval() 安全漏洞 (Blocker B1)

- 将 `_safe_eval_expr` 函数和相关常量提取到独立模块 `utils/safe_eval.py`
- `balance_checker.py` 改为 `from utils.safe_eval import safe_eval` 引用
- 安全求值器使用 AST 白名单校验，仅允许：数字字面量、算术运算、白名单函数（abs/round/min/max/pow/sqrt/floor/ceil/log 等）、常量（pi/e）
- 拒绝属性访问、import、exec、lambda、推导式等一切危险操作
- 测试文件 `tests/test_balance_template.py` 更新为导入 `from utils.safe_eval import safe_eval`
- 所有 53 个测试用例通过（含 15 个安全攻击向量拒绝测试）
