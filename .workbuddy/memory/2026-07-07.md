## 2026-07-07

### v1.39.1 修复

**Bug Fix: safe_eval 跨包导入失败**
- AstrBot 插件加载器不支持 `from ..utils.safe_eval` 跨包相对导入
- 将 `safe_eval.py` 从 `utils/` 移至 `features/`
- `balance_checker.py` 改为 `from .safe_eval import safe_eval`（同包导入）

**Bug Fix: 风格学习 JSON 解析失败**
- `_manual_extract_strings` 中 `if tail and tail[0] in ',]'` 在数组末尾元素时 `tail` 为空，将闭合引号误判为内嵌引号
- 改为 `if not tail or tail[0] in ',]'`
- 增加 JSONDecodeError 详细日志（异常信息 + JSON 片段）

**维护**
- 更新 `agent.md` 项目结构树：`learning_style/` → `features/learning_style/`，`safe_eval.py` → `features/`
- 版本号 v1.39.0 → v1.39.1
- 更新 CHANGELOG
- 提交 git commit `f033b0d`
